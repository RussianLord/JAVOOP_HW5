public class Worker extends Stuff{
    public Worker(int id, String position, int salary, String firstName, String secondName, int age) {
        super(id, position, salary, firstName, secondName, age);
    }
}
