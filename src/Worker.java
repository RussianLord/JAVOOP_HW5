public class Worker extends Stuff{

    public Worker(String position, int salary, String firstName, String secondName, int age) {
        super(position, salary, firstName, secondName, age);
    }
}
